/*
OPTIONAL: Add these methods to your RoomService if you prefer service-based approach

bulkUploadRooms(formData: FormData) {
  return this.http.post<any>(`${this.apiUrl}/rooms/bulk-upload`, formData);
}

getBulkUploadTemplate() {
  return this.http.get<any>(`${this.apiUrl}/rooms/bulk-upload/template`);
}

Note: Component already works with direct HttpClient - these are optional.
*/
